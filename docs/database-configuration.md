To be able to connect, you have to:

- Make the database publicly accessible
- Make an actual database name, not just the DB host name
- Change the security group to allow TCP connections on port 5432
